# Phase 3.2: Encrypt and Decrypt Pipelines

**Parent roadmap**: [roadmap.md](roadmap.md)  
**Design sections**: [Chunk Size](../design.md#chunk-size), [Padding Scheme](../design.md#padding-scheme), [Encrypt Pipeline](../design.md#encrypt-pipeline), [File Key Lifecycle](../design.md#file-key-lifecycle)  
**Contract anchor**: [Contract Surface](../design.md#contract-surface) in the parent `design.md` is canonical for API/wire contracts; keep this sub-phase focused on implementation sequencing and tests.  
**Depends on**: [Phase 3.1](3.1-schema-and-metadata-store.md) (needs `MetadataStore`, `ChunkRecord`, `StorageError`), Phase 1.2 (`encrypt_chunk`/`decrypt_chunk`), Phase 1.3 (key wrapping, BLAKE3)

---

## Deliverables

1. Implement `encrypt_file` in `src-tauri/src/storage/pipeline.rs` using the canonical API/flow in [Encrypt Pipeline](../design.md#encrypt-pipeline): streaming I/O, zero-pad, AEAD encrypt, BLAKE3 over encrypted blob, staging write, plaintext zeroization
2. Implement `decrypt_file` using the canonical API/flow in [Encrypt Pipeline](../design.md#encrypt-pipeline): checksum-before-decrypt, AEAD decrypt, last-chunk truncation, plaintext zeroization
3. File key lifecycle integration in `src-tauri/src/storage/vault_ops.rs`: generate `file_key` (random 256-bit via CSPRNG) → wrap with `key_encryption_key` → begin SQLCipher transaction → insert node row → encrypt all chunks → insert chunk rows → commit → zeroize `file_key`
4. File access flow: unwrap `file_key` from `nodes` table → decrypt chunks in `chunk_index` order → zeroize `file_key` on completion or error
5. Streaming invariant enforcement: at no point is more than one chunk's plaintext in memory; verified by test
6. Hybrid routing gate: when `epoch_buffer_enabled` is enabled, files smaller than `chunk_size_bytes` route to epoch buffering, while files `>= chunk_size_bytes` remain on the immediate standalone chunk path
7. Test suite covering: chunk boundary cases (0 bytes, 1 byte, `chunk_size - 1`, `chunk_size`, `chunk_size + 1`, exact multiples, multi-chunk files), BLAKE3 mismatch rejection (error returned before `decrypt_chunk` is called), UUID v4 blob naming (blob files in staging are named `<uuid>.blob`), full file round-trip (encrypt then decrypt produces byte-identical output), 0-byte file (no chunks produced, no staging files created), hybrid routing behavior
8. Property-based tests using `proptest`: arbitrary file sizes (0 to 20 MiB) round-trip correctly through encrypt/decrypt

---

## Validation Checkpoint

**Automated tests**:
```bash
cargo test storage::pipeline
```
All tests including property-based tests must pass.

**Manual verification**:
- Encrypt a multi-chunk file (e.g., 10 MiB); inspect the staging directory — confirm blob count matches `ceil(10 MiB / chunk_size_bytes)`, each file is exactly `chunk_size_bytes + 40 bytes` (chunk payload + 24-byte nonce + 16-byte Poly1305 tag)
- Corrupt one byte in a staging blob; attempt decrypt — confirm `StorageError::ChecksumMismatch` is returned

**Acceptance criteria**:
- All chunk boundary test cases pass
- BLAKE3 mismatch is detected and returned as an error before any decryption is attempted
- After `encrypt_file` returns, no plaintext chunk buffer remains in heap memory (zeroize called)
- After `decrypt_file` returns (success or error), no plaintext chunk buffer remains in heap memory
- 0-byte file: `encrypt_file` returns `Ok(vec![])` with no staging files created
- Hybrid routing is enforced when epoch buffering is enabled (small files buffered, large files immediate)

---

## Estimated Scope

- **Production code**: ~200 lines
- **Test code**: ~200 lines

---

## Implementation Notes

- AAD construction per the crypto design: `file_id` (16 bytes, UUID as bytes) `||` `chunk_index` (u32, big-endian, 4 bytes). This must match the construction in `encrypt_chunk`/`decrypt_chunk` exactly — mismatched AAD causes AEAD authentication failure.
- Blob naming: `Uuid::new_v4().to_string()` — the blob file is written as `<uuid>.blob`. The `blob_name` field in `ChunkRecord` stores only the UUID string (no `.blob` extension); the extension is added when constructing file paths.
- Zeroize: use the `zeroize` crate on the plaintext `Vec<u8>` buffer. The buffer must be zeroized even on the error path — use a guard pattern or explicit cleanup in error branches.
- `proptest` strategy for file sizes: `0usize..=(20 * 1024 * 1024)`. Generate a random byte sequence of that length, write to a `tempfile`, encrypt, decrypt, assert byte equality.
- For the 0-byte file case, `encrypt_file` reads 0 bytes and returns immediately with an empty `Vec<ChunkRecord>`. No staging file is created, no AEAD call is made.
- `decrypt_file` must sort `chunks` by `chunk_index` before processing — do not assume the caller provides them in order.

---

## Security Review

**Required** — Invoke `security-reviewer` agent after implementation:
- Streaming invariant: confirm no code path accumulates more than one chunk's plaintext in memory
- Zeroization: confirm plaintext buffers are zeroized on both success and error paths
- BLAKE3 pre-decrypt verification: confirm `decrypt_chunk` is never called if the checksum does not match

---

## Implementation Decisions

- Implemented pipeline and orchestration as split modules (`storage/pipeline/*`, `storage/vault_ops/*`) instead of single files to preserve one-concern-per-file boundaries; deferred follow-up: keep future 3.3 additions in dedicated files rather than growing these modules.
- Kept decrypt chunk-size sourcing through manifest metadata (`get_meta("chunk_size_bytes")`) in `decrypt_file(..., metadata_store)` to keep the same source of truth as encryption; deferred follow-up: if contract signatures are narrowed later, preserve this invariant in the wrapper boundary.
- Added `MetadataStore::insert_file_with_chunks` and used it from `upload_file` so node and chunk persistence is atomic; deferred follow-up: Phase 3.3 can extend this pattern to additional multi-step storage mutations.
- Enforced hybrid routing with an explicit epoch-buffer deferral error when `epoch_buffer_enabled=true` and `file_size < chunk_size_bytes`; deferred follow-up: replace this branch with real epoch-buffer packing in Phase 4.
- Hardened decrypt output finalization by writing to a temporary file and preserving the prior destination when replacement fails; deferred follow-up: evaluate a fully atomic cross-platform replace primitive when available.

---

## Next Sub-Phase

**[Phase 3.3: Staging Directory and Error Recovery](3.3-staging-and-error-recovery.md)**
- Depends on: Phase 3.2 (encrypt/decrypt pipelines), Phase 3.1 (SQLCipher manifest access for orphan detection)
- Implements: Staging directory management, orphan blob cleanup on startup, and transaction-based crash recovery
