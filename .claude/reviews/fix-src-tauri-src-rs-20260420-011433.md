# Review Fix Report — src-tauri/src/**/*.rs

> Generated by `/implement-review`
> Timestamp (UTC): 2026-04-20 01:14:33
> Source review: `.claude/reviews/review-all-rust-20260419-230316.md`
> Scope: `src-tauri/src/**/*.rs`
> Track: `full`

## Implementation Context Snapshot

- Highest implemented phase: 4.5
- In-progress phases: none
- Planned/draft phases: 6
- Key plan/handoff files consulted: `.claude/plans/phase-0-1-*.md` through `.claude/plans/phase-4-5-*.md`

## Triage Summary

- Actionable now: 24
- Conflict with approved rationale: 0
- Deferred by roadmap/plan: 1
- Intentional decisions preserved: 0

## Fixes Applied

### CF-004 — Windows staging ACL hardening
- **Finding IDs**: CF-004
- **Files changed**: `src-tauri/src/auth/staging.rs`, `src-tauri/Cargo.toml`
- **Change summary**: Added explicit Windows owner-only DACL creation/hardening for staging directory and staging file writes, while preserving Unix `0o600` handling.
- **Why now**: High-severity auth boundary leak in active phase surface.
- **Risk reduced**: Eliminates permissive ACL exposure window for pending vault-header staging artifacts.

### CF-008 — Argon2 trusted-parameter preservation
- **Finding IDs**: CF-008
- **Files changed**: `src-tauri/src/auth/ceremonies/{helpers,types,create,change_password,rotate_key_file,setup_recovery,test_support,recover_with_phrase,recover_vault,mod}.rs`, `src-tauri/src/auth/mod.rs`
- **Change summary**: Split new-vault default validation from existing-vault trusted-parameter resolution, added explicit migration intent, and updated ceremonies/tests to preserve trusted params unless migration is explicitly requested.
- **Why now**: High-severity security finding across auth ceremony flows.
- **Risk reduced**: Prevents silent parameter coercion/downgrade on existing vault operations.

### CF-009 — Crypto key-encapsulation tightening
- **Finding IDs**: CF-009
- **Files changed**: `src-tauri/src/crypto/hkdf.rs`, `src-tauri/src/crypto/types/mod.rs`, `src-tauri/src/auth/session/keys.rs`, ceremony callsites consuming derived keys
- **Change summary**: Restricted raw HKDF expansion visibility, moved callers toward wrapper-based key use with closure-scoped exposure, and reduced broad raw-byte access patterns.
- **Why now**: High-severity interface boundary concern spanning crypto/auth call paths.
- **Risk reduced**: Reduces accidental cross-shard key material exposure opportunities.

### CF-016 — SQLCipher boundary escape-hatch removal
- **Finding IDs**: CF-016
- **Files changed**: `src-tauri/src/storage/sqlcipher.rs`, `src-tauri/src/storage/cloud/{destination_session,sync,manifest_backup}.rs`
- **Change summary**: Removed generic mutable-connection escape hatch and replaced usage with named SQLCipher-specific helpers for required operations.
- **Why now**: High-severity storage boundary invariant bypass.
- **Risk reduced**: Restores explicit invariant-preserving storage boundary.

### CF-026 — pending_deletions deterministic drain + ack
- **Finding IDs**: CF-026
- **Files changed**: `src-tauri/src/storage/cloud/sync.rs`
- **Change summary**: Added queue drain orchestration with stable batched reads, delete confirmation, `mark_deletion_complete` only on success, and retry-safe preservation on failures.
- **Why now**: High-severity sync/storage correctness and durability issue.
- **Risk reduced**: Prevents deletion queue stagnation and preserves crash-safe retry behavior.

## Architecture Outcome

- Structural findings before: 6 (from source review)
- Structural findings after: unavailable (re-review agents blocked by API rate limit)
- Cross-shard issues found in remediation loops: 0
- Remaining structural blockers: re-review (`rust-reviewer`/`architecture-reviewer`) failed with model 429 rate-limit errors

## Design Challenge Ledger

### Manifest backup no-AAD contract challenge (CF-013 related)
- **Challenged constraint**: manifest backup no-AAD contract
- **Resolution**: Rejected (kept canonical no-AAD contract)
- **Implementation effect**: No design-contract change applied; CF-013 remains design-adjudication scoped
- **Follow-up owner**: human gate (`/plan` or design review flow if contract change is desired)

## Finding Overrides

None.

## Validation Summary

- `cargo check --workspace`: passed
- `cargo fmt --all -- --check`: passed (after formatting)
- `cargo clippy --workspace --all-targets --all-features -- -D warnings`: passed
- `cargo test --workspace --all-targets --all-features`: passed
- Re-review cycle reviewers: blocked by external model 429 rate limits

## Deferred / Not Applied (by design)

### CF-013 manifest backup AAD tension
- **Reason**: Requires explicit design adjudication; canonical contract currently specifies no AAD.
- **Plan citation**: `DESIGN_INDEX` (`D-024`) and storage rule anchor (`manifest backup no-AAD`)
- **Follow-up phase**: N/A (requires design decision first)

### Remaining medium/low actionable findings from source report
- **Reason**: Not part of this high-severity remediation pass.
- **Plan citation**: Source review remediation order and current implemented-phase focus.
- **Follow-up phase**: Future scoped `/implement-review` or formal `/plan` backlog item.

## Appendix

- Files reviewed/changed:
  - `src-tauri/Cargo.toml`
  - `src-tauri/src/auth/ceremonies/change_password.rs`
  - `src-tauri/src/auth/ceremonies/create.rs`
  - `src-tauri/src/auth/ceremonies/helpers.rs`
  - `src-tauri/src/auth/ceremonies/mod.rs`
  - `src-tauri/src/auth/ceremonies/recover_vault.rs`
  - `src-tauri/src/auth/ceremonies/recover_with_phrase.rs`
  - `src-tauri/src/auth/ceremonies/rotate_key_file.rs`
  - `src-tauri/src/auth/ceremonies/setup_recovery.rs`
  - `src-tauri/src/auth/ceremonies/test_support.rs`
  - `src-tauri/src/auth/ceremonies/types.rs`
  - `src-tauri/src/auth/mod.rs`
  - `src-tauri/src/auth/session/keys.rs`
  - `src-tauri/src/auth/staging.rs`
  - `src-tauri/src/crypto/hkdf.rs`
  - `src-tauri/src/crypto/types/mod.rs`
  - `src-tauri/src/storage/cloud/destination_session.rs`
  - `src-tauri/src/storage/cloud/manifest_backup.rs`
  - `src-tauri/src/storage/cloud/sync.rs`
  - `src-tauri/src/storage/sqlcipher.rs`
- Shards touched: `shard-auth`, `shard-crypto`, `shard-storage`
- Agent chain: `plan-context-builder`, `rules-extractor`, `design-extractor`, `shard-planner`, `problem-solver` (CF-004/008/009/016/026), `rust-implementer` (CF-004/008/016/026 + partial CF-009 pass), manual compile-fix follow-up, validation suite
- Rule/design anchors used: auth/crypto/storage canonical rules + design invariants
- Run state path: `.claude/reviews/fix-src-tauri-src-rs-20260420-001903/`
