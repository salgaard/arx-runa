# Phase 6.2: Frontend State Contexts and Tauri Invoke Wrapper

**Parent roadmap**: [roadmap.md](roadmap.md)  
**Design sections**: [How to Read This Design](../design.md#how-to-read-this-design), [Scope Profiles](../design.md#scope-profiles), [Canonical Command Surface (Normative)](../design.md#canonical-command-surface-normative), [State Contexts](../design.md#state-contexts), and [Tauri IPC Integration](../design.md#tauri-ipc-integration) of [design.md](../design.md)  
**Contract anchor**: [Contract Surface](../design.md#contract-surface) in the parent `design.md` is canonical for interface/data/invariant/dependency contracts; keep this sub-phase focused on implementation sequencing and validation.  
**Depends on**: [Phase 6.1](6.1-ipc-core-and-error-sanitisation.md) (IPC response types required for frontend deserialisation), and `tauri.conf.json` preconfigured with `withGlobalTauri: true`

---

## Deliverables

1. `invoke_command<A, R>(cmd: &str, args: A) -> Result<R, IpcError>` generic async function in `src/lib.rs`, wrapping `window.__TAURI__.core.invoke` via `wasm_bindgen` and deserialising the result with `serde_wasm_bindgen`; command names consumed by wrappers/hooks must align with [Canonical Command Surface (Normative)](../design.md#canonical-command-surface-normative)
2. Frontend `IpcError` struct in `src/lib.rs` with `kind: String` and `message: String` fields, deserialised from the `{"kind": "...", "message": "..."}` JSON shape produced by the backend
3. `SessionProvider` component in `src/state/session_context.rs`: polls `get_session_status` every 5 seconds via `spawn_local` + `gloo_timers`, registers a stop signal on `on_cleanup` to prevent stale intervals after unmount
4. `SessionState` signal fields: `is_unlocked: bool`, `vault_id: Option<String>`, `timeout_seconds: Option<u64>`, `authenticating: bool`, `error: Option<String>`
5. `use_session()` accessor hook and `use_session_actions()` hook, both backed by `provide_context` / `use_context`, panicking with a clear message if called outside a `SessionProvider`
6. `VaultState` signal in `src/state/vault_context.rs` with fields: `current_path: String`, `files: Vec<FileEntry>`, `loading: bool`, `error: Option<String>`, `selected: Vec<String>`
7. `VaultActions` struct with `navigate(path: String)` (calls `list_directory` IPC and updates `files` and `current_path`) and `clear()` (wipes all `VaultState` fields to defaults — used on lock)
8. `SyncContext` in `src/state/sync_context.rs` with `SyncStatus` signal tracking current sync state (idle, running, conflict, error)
9. Unit tests: `VaultActions::clear()` resets all fields to defaults; `SessionState` transitions from `authenticating: true` to `is_unlocked: true` on success, and to `error: Some(...)` on failure

---

## Validation Checkpoint

**Automated tests**:
```bash
trunk build
```

Unit tests (run via `wasm-pack test` or equivalent Leptos test harness):
- `VaultActions::clear()` zeroes `files`, `current_path`, `selected`, and `error`
- `SessionState` transitions behave correctly for success and failure paths

**Acceptance criteria**:
- `trunk build` completes without errors or warnings
- State contexts provide and consume without panic in a browser environment
- `invoke_command` correctly deserialises a success response into `R` and an error response into `IpcError`

---

## Estimated Scope

- **Production code**: ~250 lines
- **Test code**: ~50 lines

---

## Implementation Notes

- `invoke_command` uses `serde_wasm_bindgen::from_value` for deserialisation; errors from Tauri IPC arrive as a serialised `IpcError` JSON object on the rejected promise, which must be deserialised separately from success responses
- `gloo_timers::future::TimeoutFuture` is the preferred timer for polling inside `spawn_local`; the stop signal should be a `Rc<Cell<bool>>` that the cleanup closure sets to `true`
- `use_context::<SessionState>()` returns `Option<...>` in Leptos; the `use_session()` hook should `.expect("SessionProvider not found in component tree")` to give a clear error message during development
- `FileEntry` on the frontend mirrors the backend `types.rs` definition; keep the two in sync manually until a shared type generation step is introduced
- `withGlobalTauri: true` is a precondition for this phase because the invoke wrapper calls `window.__TAURI__.core.invoke`; set it before Phase 6.2 and keep Phase 6.4 focused on verification/hardening

---

## Security Review

**Not required** — this sub-phase contains no cryptographic operations, no key material handling, and no IPC surface definition. Frontend state management only.

---

## Next Sub-Phase

**[Phase 6.3: Frontend Pages](6.3-frontend-pages.md)**
- Depends on: Phase 6.2 (state contexts and invoke wrapper)
- Implements: `LoginPage`, `VaultBrowser`, `ProgressModal`, `AppShell`, and generic UI components
