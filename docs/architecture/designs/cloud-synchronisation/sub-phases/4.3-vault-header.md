# Phase 4.3: Vault Header Upload and Download

**Parent roadmap**: [roadmap.md](roadmap.md)  
**Design sections**: [Vault Header](../design.md#vault-header), [Upload flow](../design.md#upload-flow), [Download and parse flow](../design.md#download-and-parse-flow), [Security analysis of plaintext fields](../design.md#security-analysis-of-plaintext-fields)  
**Contract anchor**: [Contract Surface](../design.md#contract-surface) in the parent `design.md` is canonical for interface/data/invariant/dependency contracts; keep this sub-phase focused on implementation sequencing and validation.  
**Depends on**: [Phase 4.2](4.2-rclone-integration.md) (RcloneTransport)

---

## Deliverables

1. `VaultHeader` struct in `src-tauri/src/storage/cloud/vault_header.rs` with fields: `vault_id`, `schema_version`, `tier`, `argon2_salt`, `argon2_params`, `key_file_blake3`, `recovery_slots`
2. `Argon2Params` struct with `memory_cost`, `time_cost`, `parallelism` fields
3. `RecoverySlot` struct with fields: `method`, `argon2_salt`, `argon2_params`, `wrapped_master_key` — see `design.md` for full definition and field constraints
4. `upload_vault_header` function: serialize `VaultHeader` to JSON → write to temp file → `upload_blob(temp_path, "vault-header.json")` → delete temp
5. `download_vault_header` function: `download_blob("vault-header.json", temp_path)` → read and deserialize → validate schema version, salt length, Argon2 params, recovery slots → delete temp
6. Validation logic — use the [Download and parse flow](../design.md#download-and-parse-flow) from `design.md`. Key rules: `schema_version` is supported; `tier` is 1 or 2; `argon2_salt` decodes to 32 bytes; `argon2_params` meet minimums; `key_file_blake3` is required (32-byte hex) for tier 2 and must be `null` for tier 1; each recovery slot's `argon2_salt` decodes to 32 bytes, `argon2_params` meet minimums, `wrapped_master_key` decodes to 72 bytes, unknown `method` values are silently skipped. Do not reproduce the full validation sequence here — consult `design.md` during implementation.
7. Tests: Upload → download round-trip, validation rejects malformed headers, round-trip preserves recovery slots

---

## Validation Checkpoint

**Automated tests**:
```bash
cargo test storage::cloud::vault_header
```

**Manual verification**:
- Upload vault header to real cloud provider
- Download from cloud and verify JSON parses correctly
- Inspect `vault-header.json` in cloud bucket — confirm it's plaintext JSON

**Acceptance criteria**:
- Vault header uploads as plaintext JSON
- New-device recovery flow can download and parse vault header before authentication
- Validation correctly rejects malformed headers

---

## Estimated Scope

- **Production code**: ~120 lines (struct + serialize + validate)
- **Test code**: ~80 lines

---

## Implementation Notes

- Use `serde_json` for serialization/deserialization
- Base64 encoding: use `base64` crate with standard alphabet
- Hex encoding: use `hex` crate for `key_file_blake3`
- Temp files should be written to Arx Runa staging directory (from Phase 3), not system temp
- Vault header is intentionally plaintext — do not encrypt (needed for bootstrap)

---

## Security Review

**Not required** (plaintext by design; security analysis in design document validates this approach)

---

## Next Sub-Phase

**[Phase 4.4: Manifest Cloud Backup](4.4-manifest-backup.md)**
- Depends on: Phase 4.3 (vault header), Phase 1 (crypto primitives for manifest_key)
- Implements: Encrypted manifest backup for full vault recovery
