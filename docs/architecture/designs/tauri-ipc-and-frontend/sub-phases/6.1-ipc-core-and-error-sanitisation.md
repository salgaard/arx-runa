# Phase 6.1: IPC Core, Error Sanitisation, and Types

**Parent roadmap**: [roadmap.md](roadmap.md)  
**Design sections**: IPC Layer Architecture / Command Organisation (lines 26-37), Command Signatures (lines 40-123), Command Registration (lines 125-177), Error Sanitisation (lines 180-307), IPC Response Types (lines 310-417), Application State (lines 419-463), Input Validation (lines 466-506) of [design.md](../design.md)  
**Depends on**: Phase 2 (auth module), Phase 3 (storage module), Phase 4 (cloud transport), Phase 5 (sharing module) — these provide the backend implementations the commands delegate to

---

## Deliverables

1. Module structure under `src-tauri/src/ui/`: `mod.rs`, `error.rs`, `auth_commands.rs`, `file_commands.rs`, `sync_commands.rs`, `types.rs`
2. `IpcError` enum in `error.rs` with `#[serde(tag = "kind", content = "message")]` and `#[serde(rename_all = "camelCase")]`: variants `VaultLocked`, `AuthenticationFailed`, `NotFound`, `AlreadyExists`, `CloudError`, `InvalidInput`, `InternalError`
3. `From<auth::AuthenticationError> for IpcError`: logs full error via `tracing::error!`, maps `InvalidCredentials` → `AuthenticationFailed("Invalid credentials")`, `KeyFileNotFound` → `AuthenticationFailed("Key file not found")`, `MemoryLockFailed` → `InternalError("Cannot lock memory for session keys")`, `VaultHeaderInvalid` → `InternalError("Vault configuration error")`, catch-all → `InternalError("An error occurred")`
4. `From<storage::StorageError> for IpcError`: logs full error, maps `FileNotFound` → `NotFound`, `DirectoryNotFound` → `NotFound`, `AlreadyExists` → `AlreadyExists`, catch-all → `InternalError`
5. `From<sync::SyncError> for IpcError`: logs full error, maps `NetworkUnavailable` → `CloudError("Network unavailable")`, `CloudProviderError` → `CloudError("Cloud provider error")`, catch-all → `InternalError`
6. Sanitisation rules enforced by the `From` impls: never expose file paths, key material, memory addresses, stack traces, or specific crypto errors in any message string
7. All response types in `types.rs` — use the [IPC Response Types](../design.md#ipc-response-types) from `design.md`. Do not reproduce the full struct definitions here — consult `design.md` during implementation. Key types: `AuthResponse` (`vault_id`, `vault_name`), `SessionStatus` (`is_unlocked`, `vault_id: Option<String>`, `timeout_seconds: Option<u64>`), `FileEntry` (`id`, `name`, `entry_type: String`, `size_bytes: u64`, `modified_at: String`, `parent_id: Option<String>`), `ProgressUpdate` (`percent: u8`, `bytes_processed`, `bytes_total`, `status: String`), plus `SyncProgressUpdate`, `SyncResult`, `SyncConflict`, `SyncStatus`, `FileContent`, `ShareResponse`, `ImportShareResponse`, `ContactEntry`, `ShareEntry`, `ReceivedShareEntry`, `MigrationProgress`, `CloudEndpointConfig`
8. `AppState` struct in `mod.rs`: `database: Arc<RwLock<Option<DatabaseConnection>>>`, `cloud_transport: Arc<dyn CloudTransport>`, `device_monitor: Arc<dyn DeviceMonitor>`, `session_manager: Arc<SessionManager>`, `sync_status: Arc<RwLock<SyncStatus>>`
9. Input validation functions in `mod.rs` or a dedicated `validation.rs`: `validate_vault_path` (rejects path traversal `..`, absolute paths, and control characters), `validate_file_id` (UUID v4 format check), `validate_password` (non-empty check), `validate_chunk_size` (rejects values outside 131072–67108864; returns `IpcError::InvalidInput` with a message stating the allowed range)
10. All 24 Tauri command signatures registered via `tauri::generate_handler![]` in `src-tauri/src/lib.rs` — see [Command Registration](../design.md#command-registration) in `design.md` for the full `invoke_handler` block. Auth (7): `authenticate`, `create_vault`, `change_password`, `rotate_key_file`, `delete_vault`, `lock_session`, `get_session_status`. File (5): `list_directory`, `upload_file`, `download_file`, `delete_file`, `get_file_content`. Sync (4): `sync_to_cloud`, `recover_from_cloud`, `get_sync_status`, `migrate_vault`. Sharing (8): `export_public_key`, `add_contact`, `list_contacts`, `share_file`, `import_share`, `revoke_share`, `list_shares`, `list_received_shares`. Note: `create_vault` now accepts two additional parameters — `chunk_size_bytes: u64` (validated by `validate_chunk_size`) and `epoch_buffer_enabled: bool` — see [Command Signatures](../design.md#command-signatures) in `design.md`.
11. Build-time allowlist in `src-tauri/build.rs` listing all 24 command names via `tauri_build::AppManifest::new().commands(&[...])` — see [Command Registration](../design.md#command-registration) in `design.md` for the exact allowlist.

---

## Validation Checkpoint

**Automated tests**:
```bash
cargo test ui::error
cargo test ui::validation
cargo build
```

`cargo test ui::error` must pass for:
- `AuthenticationError::InvalidCredentials` maps to `IpcError::AuthenticationFailed` with message `"Invalid credentials"`
- `AuthenticationError::KeyFileNotFound` maps to `IpcError::AuthenticationFailed` with message `"Key file not found"`
- `AuthenticationError::MemoryLockFailed` maps to `IpcError::InternalError` with message `"Cannot lock memory for session keys"`
- `AuthenticationError::VaultHeaderInvalid` maps to `IpcError::InternalError` with message `"Vault configuration error"`
- `StorageError::FileNotFound` maps to `IpcError::NotFound` with no path in message
- `StorageError::AlreadyExists` maps to `IpcError::AlreadyExists` with no path in message
- `SyncError::NetworkUnavailable` maps to `IpcError::CloudError`
- No message string contains `/`, `\`, `key`, `0x`, or a hex sequence of 32+ characters

`cargo test ui::validation` must pass for:
- `validate_vault_path` rejects `../escape`, `/absolute/path`, `path\with\backslash`, and strings containing `\x00`–`\x1F`
- `validate_file_id` rejects malformed UUIDs and empty strings
- `validate_password` rejects empty strings
- `validate_chunk_size` rejects values below 131072 (128 KiB), above 67108864 (64 MiB), and zero

`cargo build` must succeed with all 24 commands registered (compile-time allowlist check).

**Acceptance criteria**:
- `IpcError` serialises correctly to `{"kind": "notFound", "message": "..."}` JSON shape
- All `From` impls log the full internal error before returning a sanitised message
- No internal error details (paths, addresses, crypto specifics) appear in any `IpcError` message

---

## Estimated Scope

- **Production code**: ~400 lines
- **Test code**: ~150 lines

---

## Implementation Notes

- `IpcError` must implement `serde::Serialize` (not `Deserialize`) — it is only ever sent backend → frontend
- The `serde(tag = "kind", content = "message")` shape is required so the frontend `IpcError` struct can pattern-match on `kind` as a string discriminant
- Catch-all arms in `From` impls must use `tracing::error!` with `{:?}` (Debug format) to capture full context server-side, then return a fixed generic message — never interpolate the original error into the returned `IpcError`
- `AppState` fields use `Arc` so the state can be cloned across async command handlers; `RwLock` is used where shared mutable access is needed
- `validate_vault_path` should normalise the path and check the result does not escape a known root prefix — do not rely solely on the absence of `..` literals

---

## Security Review

**Required** — Invoke `security-reviewer` agent after implementation:
- Verify that every `From` impl arm produces a message free of file paths, key material, memory addresses, stack traces, and specific crypto error details
- Verify that catch-all arms do not interpolate any portion of the original error into the returned message
- Verify that `validate_vault_path` blocks all traversal forms including URL-encoded variants and null bytes

---

## Next Sub-Phase

**[Phase 6.2: Frontend State Contexts and Tauri Invoke Wrapper](6.2-frontend-state-and-invoke-wrapper.md)**
- Depends on: Phase 6.1 (IPC response types needed for frontend deserialisation)
- Implements: Type-safe `invoke_command` wrapper and Leptos state contexts (`SessionProvider`, `VaultState`, `SyncContext`)
