//! Error types for the ui module.

use thiserror::Error;

/// Errors produced by the ui module.
#[non_exhaustive]
#[derive(Debug, Error)]
pub enum UiError {
    // Variants added in implementation phases.
}
